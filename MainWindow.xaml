<Window
    x:Class="JakeyTTS.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
            <RowDefinition x:Name="LogRow" Height="150"/>
        </Grid.RowDefinitions>

        <!-- CUSTOM TITLE BAR -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" VerticalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                <Image Source="ms-appx:///app.ico" Width="20" Height="20" Margin="0,0,12,0" Stretch="Uniform"/>
                <TextBlock Text="JakeyTTS" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}"
                           FontSize="13" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            </StackPanel>
        </Grid>

        <!-- NAVIGATION -->
        <NavigationView x:Name="NavView" 
                        Grid.Row="1"
                        PaneDisplayMode="Left"
                        IsSettingsVisible="True"
                        IsBackButtonVisible="Collapsed" 
                        ItemInvoked="NavView_ItemInvoked">

            <NavigationView.MenuItems>
                <!-- Standard icons can still use the shorthand -->
                <NavigationViewItem Content="Home" Tag="Home" Icon="Home"/>
                <NavigationViewItem Content="TTS Settings" Tag="TTSConfig" Icon="People"/>

                <!-- New icons require the FontIcon syntax -->
                <NavigationViewItem Content="User Actions" Tag="UserActions">
                    <NavigationViewItem.Icon>
                        <!-- Contact2 Icon -->
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xEA4A;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="History" Tag="History" >
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE81C;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Commands" Tag="Commands">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE90A;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Redeems" Tag="Rewards" Icon="OutlineStar"/>

                <!-- New MusicInfo Icon -->
                <NavigationViewItem Content="Melodies" Tag="Melodies">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8D6;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Sound Effects" Tag="SFX" >
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xF61F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Mixed Voices" Tag="MixedVoices" Icon="Volume"/>

                <NavigationViewItemSeparator/>

                <NavigationViewItem Content="Keybinds" Tag="Keybinds" Icon="Keyboard"/>

                <!-- New Puzzle (Extension) Icon -->
                <NavigationViewItem Content="Plugins &amp; Integrations" Tag="Plugins">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xEA86;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.PaneFooter>
                <NavigationViewItem x:Name="LogToggleBtn" Content="Hide Log" Icon="List" 
                                    SelectsOnInvoked="False" Tapped="LogToggle_Tapped"/>
            </NavigationView.PaneFooter>

            <Frame x:Name="ContentFrame" Margin="24,0,24,0"/>
        </NavigationView>

        <!-- LOG CONSOLE -->
        <Border x:Name="LogBorder" Grid.Row="2" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <Grid Padding="12">
                <ScrollViewer x:Name="LogScroll">
                    <TextBox x:Name="LogBlock" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                             Background="Transparent" BorderThickness="0" FontSize="12" FontFamily="Cascadia Code, Consolas"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>