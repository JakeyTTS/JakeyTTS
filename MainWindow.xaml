<Window
    x:Class="JakeyTTS.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <!-- Removed Header="Jakey TTS" -->
        <NavigationView x:Name="NavView" 
                        Grid.Row="0"
                        PaneDisplayMode="Left"
                        IsSettingsVisible="True"
                        ItemInvoked="NavView_ItemInvoked">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home" Tag="Home" Icon="Home"/>
                <NavigationViewItem Content="TTS Settings" Tag="TTSConfig" Icon="People"/>
                <NavigationViewItem Content="History" Tag="History" Icon="Clock"/>
                <NavigationViewItem Content="Commands" Tag="Commands" Icon="Go"/>
                <NavigationViewItem Content="Rewards" Tag="Rewards" Icon="OutlineStar"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" Margin="24,0,24,24"/>
        </NavigationView>

        <Border Grid.Row="1" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <Grid Padding="12">
                <ScrollViewer x:Name="LogScroll">
                    <TextBox x:Name="LogBlock" 
                             IsReadOnly="True" 
                             TextWrapping="Wrap" 
                             AcceptsReturn="True"
                             Background="Transparent" 
                             BorderThickness="0"
                             FontSize="12"
                             FontFamily="Cascadia Code, Consolas"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>