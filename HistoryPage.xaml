<Page
    x:Class="JakeyTTS.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JakeyTTS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Recent Activity" 
                   Style="{StaticResource SubtitleTextBlockStyle}" 
                   Margin="0,0,0,20"/>

        <ListView x:Name="HistoryListView" 
                  Grid.Row="1"
                  ItemsSource="{x:Bind History}"
                  SelectionMode="None">

            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Time" FontWeight="Bold" Foreground="Gray"/>
                        <TextBlock Text="User" FontWeight="Bold" Foreground="Gray" Grid.Column="1"/>
                        <TextBlock Text="Message" FontWeight="Bold" Foreground="Gray" Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TtsEntry">
                    <Grid Padding="8,12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{x:Bind Time}" 
                                   Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" 
                                   VerticalAlignment="Center"/>

                        <TextBlock Text="{x:Bind User}" 
                                   FontWeight="SemiBold" 
                                   Grid.Column="1" 
                                   VerticalAlignment="Center" 
                                   Margin="8,0"/>

                        <TextBlock Text="{x:Bind Message}" 
                                   Grid.Column="2" 
                                   VerticalAlignment="Center" 
                                   TextWrapping="NoWrap" 
                                   TextTrimming="CharacterEllipsis"/>

                        <Button Grid.Column="3" 
                                Click="Replay_Click" 
                                Style="{StaticResource SubtleButtonStyle}" 
                                ToolTipService.ToolTip="Replay Voice">
                            <SymbolIcon Symbol="RepeatAll"/>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>