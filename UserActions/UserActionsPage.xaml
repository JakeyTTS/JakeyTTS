<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JakeyTTS.UserActions.UserActionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JakeyTTS.UserActions"
    xmlns:models="using:JakeyTTS.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:JakeyTTS.Core.Converters"
    mc:Ignorable="d" x:Name="UserActionsRoot">

    <Page.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Page.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="User Event Actions" Style="{StaticResource TitleTextBlockStyle}" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="15">
                <TextBlock Text="Changes apply to next event" VerticalAlignment="Center" Opacity="0.5" FontStyle="Italic" FontSize="12"/>
                <Button x:Name="SaveBtn" Content="Save All Actions" Click="Save_Click" Style="{StaticResource AccentButtonStyle}" Width="180" Height="40"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="290"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListView x:Name="CategorySelector" Grid.Column="0" SelectionMode="Single" SelectedIndex="0" Margin="0,0,20,0">
                <ListViewItem Tag="Bits">
                    <StackPanel Orientation="Horizontal" Spacing="16" Padding="4,10">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xF4A5;" FontSize="16" MinWidth="24" MinHeight="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Bits (Cheer) Alerts" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </ListViewItem>
                <ListViewItem Tag="Subs">
                    <StackPanel Orientation="Horizontal" Spacing="16" Padding="4,10">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE787;" FontSize="16" MinWidth="24" MinHeight="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Subscription Months" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </ListViewItem>
                <ListViewItem Tag="Streaks">
                    <StackPanel Orientation="Horizontal" Spacing="16" Padding="4,10">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xED0E;" FontSize="16" MinWidth="24" MinHeight="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Stream Resub Streaks" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </ListViewItem>
                <ListViewItem Tag="Goals">
                    <StackPanel Orientation="Horizontal" Spacing="16" Padding="4,10">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE960;" FontSize="16" MinWidth="24" MinHeight="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Creator Goals" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </ListViewItem>
            </ListView>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="320"/>
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" Padding="0,0,20,0">
                    <Grid>
                        <StackPanel Spacing="15" x:Name="BitsSubPage" Visibility="Collapsed">
                            <TextBlock Text="Bits Configuration" FontSize="20" FontWeight="SemiBold"/>
                            <ItemsControl ItemsSource="{Binding Config.UserActions.BitActions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:UserActionItem">
                                        <Grid ColumnSpacing="12" Margin="0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <NumberBox Header="Min Bits" Value="{Binding Threshold, Mode=TwoWay}" SpinButtonPlacementMode="Compact"/>
                                            <TextBox Grid.Column="1" Header="{}{Response (Use {user}, {bits}, {message})}" Text="{Binding Response, Mode=TwoWay}"/>
                                            <CheckBox Grid.Column="2" Content="Read Alert" IsChecked="{Binding ShouldPlayUserMessage, Mode=TwoWay}" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
                                            <Button Grid.Column="3" VerticalAlignment="Bottom" Click="DeleteBitAction_Click" Style="{StaticResource DefaultButtonStyle}" Padding="6,6" Margin="0,0,0,3">
                                                <SymbolIcon Symbol="Delete" Foreground="Red"/>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button Content="Add Bit Threshold" Click="AddBitAction_Click" Style="{StaticResource DefaultButtonStyle}"/>
                        </StackPanel>

                        <StackPanel Spacing="15" x:Name="SubsSubPage" Visibility="Collapsed">
                            <TextBlock Text="Subscriptions Configuration" FontSize="20" FontWeight="SemiBold"/>
                            <ItemsControl ItemsSource="{Binding Config.UserActions.SubActions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:UserActionItem">
                                        <Grid ColumnSpacing="12" Margin="0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <NumberBox Header="Months" Value="{Binding Threshold, Mode=TwoWay}" SpinButtonPlacementMode="Compact"/>
                                            <TextBox Grid.Column="1" Header="{}{Response ({user}, {months}, {message})}" Text="{Binding Response, Mode=TwoWay}"/>
                                            <CheckBox Grid.Column="2" Content="Read Msg" IsChecked="{Binding ShouldPlayUserMessage, Mode=TwoWay}" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
                                            <Button Grid.Column="3" VerticalAlignment="Bottom" Click="DeleteSubAction_Click" Style="{StaticResource DefaultButtonStyle}" Padding="6,6" Margin="0,0,0,3">
                                                <SymbolIcon Symbol="Delete" Foreground="Red"/>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button Content="Add Sub Threshold" Click="AddSubAction_Click" Style="{StaticResource DefaultButtonStyle}"/>
                        </StackPanel>

                        <StackPanel Spacing="15" x:Name="StreaksSubPage" Visibility="Collapsed">
                            <TextBlock Text="Resub Streaks Configuration" FontSize="20" FontWeight="SemiBold"/>
                            <ItemsControl ItemsSource="{Binding Config.UserActions.StreakActions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:UserActionItem">
                                        <Grid ColumnSpacing="12" Margin="0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <NumberBox Header="Streak" Value="{Binding Threshold, Mode=TwoWay}" SpinButtonPlacementMode="Compact"/>
                                            <TextBox Grid.Column="1" Header="{}{Response ({user}, {streak}, {message})}" Text="{Binding Response, Mode=TwoWay}"/>
                                            <CheckBox Grid.Column="2" Content="Read Msg" IsChecked="{Binding ShouldPlayUserMessage, Mode=TwoWay}" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
                                            <Button Grid.Column="3" VerticalAlignment="Bottom" Click="DeleteStreakAction_Click" Style="{StaticResource DefaultButtonStyle}" Padding="6,6" Margin="0,0,0,3">
                                                <SymbolIcon Symbol="Delete" Foreground="Red"/>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button Content="Add Streak Threshold" Click="AddStreakAction_Click" Style="{StaticResource DefaultButtonStyle}"/>
                        </StackPanel>

                        <StackPanel Spacing="15" x:Name="GoalsSubPage" Visibility="Collapsed">
                            <TextBlock Text="Twitch Creator Goals" FontSize="20" FontWeight="SemiBold"/>
                            <TextBox Header="{}{Subscription Goals Message (Use {goal_title})}" Text="{Binding Config.UserActions.SubGoalReachedResponse, Mode=TwoWay}" Margin="0,4"/>
                            <TextBox Header="{}{Follower Goals Message (Use {goal_title})}" Text="{Binding Config.UserActions.FollowerGoalReachedResponse, Mode=TwoWay}" Margin="0,4"/>
                            <TextBox Header="{}{Bits Goals Message (Use {goal_title})}" Text="{Binding Config.UserActions.BitsGoalReachedResponse, Mode=TwoWay}" Margin="0,4"/>
                            <TextBox Header="{}{Channel Points Goals Message (Use {goal_title})}" Text="{Binding Config.UserActions.PointsGoalReachedResponse, Mode=TwoWay}" Margin="0,4"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>

                <Border Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
                            <StackPanel Spacing="15">
                                <TextBlock Text="Parameter Guide" FontWeight="Bold" FontSize="15"/>
                                <TextBlock Text="You can map dynamic variables inside your text responses to contextually narrate actions:" FontSize="13" Opacity="0.7" TextWrapping="Wrap"/>

                                <StackPanel x:Name="GuideBitsBlock" Spacing="8">
                                    <TextBlock Text="• {user} - Username of the contributor" FontSize="12" Opacity="0.9"/>
                                    <TextBlock Text="• {bits} - Total number of bits cheered" FontSize="12" Opacity="0.9"/>
                                </StackPanel>

                                <StackPanel x:Name="GuideSubsBlock" Spacing="8" Visibility="Collapsed">
                                    <TextBlock Text="• {user} - Username of the subscriber" FontSize="12" Opacity="0.9"/>
                                    <TextBlock Text="• {months} - Total months accumulated" FontSize="12" Opacity="0.9"/>
                                </StackPanel>

                                <StackPanel x:Name="GuideStreaksBlock" Spacing="8" Visibility="Collapsed">
                                    <TextBlock Text="• {user} - Username of the subscriber" FontSize="12" Opacity="0.9"/>
                                    <TextBlock Text="• {streak} - Months in consecutive streak" FontSize="12" Opacity="0.9"/>
                                </StackPanel>

                                <StackPanel x:Name="GuideGoalsBlock" Spacing="8" Visibility="Collapsed">
                                    <TextBlock Text="• {goal_title} - The active description text configured on Twitch" FontSize="12" Opacity="0.9"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>

                        <Button Grid.Row="1" Click="PlayTest_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Padding="12" Height="44">
                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE768;" FontSize="14"/>
                                <TextBlock Text="Play Test Simulation" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>

            </Grid>
        </Grid>
    </Grid>
</Page>
